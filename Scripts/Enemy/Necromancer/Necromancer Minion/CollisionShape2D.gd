class_name NecromancerMinionHitboxCollision extends CollisionShape2D

@export var facing_left_position: Vector2 = Vector2(-3,0)
@export var facing_right_position: Vector2 = Vector2(4, 0)

# Called when the node enters the scene tree for the first time.
func _ready():
	pass # Replace with function body.

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
